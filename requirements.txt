# Pinned for reproducible deployment (e.g. Streamlit Community Cloud).
# scikit-learn is pinned to the version the model was trained on so the
# saved pipeline unpickles correctly.
streamlit==1.45.1
pandas==2.2.3
numpy==2.1.3
scikit-learn==1.6.1
matplotlib==3.10.0
plotly==5.24.1
joblib==1.4.2

# Used only by the offline data/analysis scripts (not required to run the app):
fastf1==3.8.3
shap==0.51.0
xgboost==3.2.0
